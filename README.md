# os-proxyman
n Nginx Proxy Manager-style reverse proxy GUI, built as a native OPNsense plugin powered by Caddy. Manage all your reverse proxy hosts, TLS certificates, and access control rules from inside the OPNsense web UI — no SSH, no config files, no fuss.
